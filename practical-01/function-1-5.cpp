//function to find even number

int count_even(int number)
{
    if (number<0)
        return 0;
    else
        int count_even=0;
        for (int i=0; i<n; i++)
        {
            if (i%2==0)
                count_even=count_even+1;
        return count_even;
        }
}
