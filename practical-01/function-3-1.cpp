#include<iostream>

bool fanararray(int array[],int n);
void main()
{
        for(int i=0;i<(n/2);i++)
        {  
           if (array[i]==array[n-i-1])
               return 0;
          
        }
        return fanarray(array,n);
}
